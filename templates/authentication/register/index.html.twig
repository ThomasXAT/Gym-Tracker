{% extends 'base.html.twig' %}
{% block body %}
        <section class="content container" {{ stimulus_controller('register') }}>
            <div id="title" class="text-center py-3">
                <h2>{{ 'authentication.register.form'|trans }}</h2>
                <p class="required">{{ 'authentication.register.required'|trans }}</p>
            </div>
{{ include('authentication/register/_form.html.twig') }}        
            <p class="text-center py-3"><a href="{{ path('authentication_login') }}">{{ 'authentication.register.link'|trans }}</a></p>
        </section>
{% endblock %}
                    <aside id="_add" class="modal fade">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <header id="_add-header" class="modal-header">
                                    <p class="modal-title fs-5">{{ 'main.session.set.add.title'|trans }}</p>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                </header>
                                <main id="_add-body" class="modal-body">
                                    {# <p class="text-center">{{ 'unit.label'|trans }} <span class="text-white">{{ ('unit.' ~ session.athlete.unit)|trans }}</span></p> #}
                                    <form id="_add-form" autocomplete="off"></form>
                                </main>
                                <footer id="_add-footer" class="modal-footer">
                                    <div class="d-flex w-100 align-items-center">
                                        <div class="col-6 text-start">
                                            <a id="button-select-exercice" class="text-decoration-none text-nowrap pointer-only" data-bs-target="#_select_exercice" data-bs-toggle="modal">{{ 'main.session.exercice.choose.action'|trans }}</a>
                                        </div>
                                        <div class="col-6 text-end">
                                            <button id="button-add-set" data-action="click->session#add" data-bs-dismiss="modal" class="btn btn-success text-nowrap"><i class="fa-solid fa-plus"></i> {{ 'main.session.set.add.submit'|trans }}</button>
                                        </div>
                                    </div>
                                </footer>
                            </div>
                        </div>
                    </aside>
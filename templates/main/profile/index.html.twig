{% extends 'base.html.twig' %}
{% block body %}
        <section class="content container">
            <h2 id="title" hidden>{{ athlete.username }}</h2>
            <div class="d-md-flex justify-content-between">
                {# Information #}<section id="information" class="col-12 col-md-4 col-xl-3 pe-md-2 py-2 mx-auto" {{ stimulus_controller('information') }}></section>
                {# Sessions #}<section id="sessions" class="col-12 col-md-8 col-xl-9 ps-md-2 py-2 d-flex">
                    <div class="card text-center flex-fill">
                        <header class="card-header">
                        </header>
                        <main class="card-body">
                            <table class="table table-dark table-hover">
                            </table>
                        </main>
                        <footer class="card-footer">
                        </footer>
                    </div>
                </section>
            </div>
        </section>
{% endblock %}
    <aside id="_new_session" class="modal fade" {{ stimulus_controller('new_session') }}>
        <div class="modal-dialog">
            <div class="modal-content">
                <form method="post" action="{{ path('session_start') }}" autocomplete="off">
                    <header class="modal-header">
                        <p class="modal-title fs-5">{{ 'main.session.new'|trans }}</p>
                        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                    </header>
                    <main class="modal-body">
                        <div class="form-floating">
                            <input type="text" id="session-title" name="title" class="form-control" placeholder="{{ 'main.session.label'|trans }}" value="Ma séance du {{ 'now'|date('d/m/Y') }}">
                            <label for="session-title" class="form-label">{{ 'main.session.label'|trans }}</label>
                            <span id="help-title" class="form-text text-danger"></span>
                        </div>
                    </main>
                    <footer class="modal-footer">
                        <button id="session-start" type="submit" class="btn btn-success text-nowrap">{{ 'main.session.start'|trans }}</button>
                    </footer>
                </form>
            </div>
        </div>
    </aside>